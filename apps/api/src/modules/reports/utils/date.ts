// Re-export from shared package
export {
  getMonthKey,
  getMonthsBetween,
  parseMonthKey,
  formatIntervalLabel,
  createIntervals,
  type Interval,
  type PeriodFormat,
} from '@fin-u-ch/shared';
