# Permission System - Система прав доступа

Гранулярная система прав доступа с автоматическим резолвингом иерархии и зависимостей между сущностями.

## 📋 Оглавление

- [Обзор](#обзор)
- [Архитектура](#архитектура)
- [Ключевые возможности](#ключевые-возможности)
- [Как это работает](#как-это-работает)
- [Добавление новой сущности](#добавление-новой-сущности)
- [Примеры использования](#примеры-использования)

## Обзор

Система предоставляет гранулярный контроль доступа на уровне сущностей и действий с автоматическим решением следующих проблем:

1. **Иерархия действий**: `delete` автоматически включает `update` и `read`
2. **Зависимости сущностей**: создание операций автоматически дает доступ на чтение справочников
3. **Централизованная конфигурация**: один источник правды для всех сущностей
4. **Type Safety**: полная поддержка TypeScript

## Архитектура

```
apps/api/src/modules/roles/
├── types.ts                      # TypeScript типы
├── config/
│   ├── entities.config.ts        # Конфигурация всех сущностей (ГЛАВНЫЙ ФАЙЛ)
│   └── action-hierarchy.ts       # Иерархия действий
├── policy/
│   └── permission-policy.ts      # Логика проверки прав
├── permissions.service.ts        # API для проверки прав
└── index.ts                      # Экспорт модуля
```

## Ключевые возможности

### 1. Иерархия действий

Высокие права автоматически включают низкие:

```typescript
delete → update → read
create → read
archive → read
confirm → read
```

**Пример**: Если у пользователя есть `operations:delete`, он автоматически может выполнять `operations:update` и `operations:read`.

### 2. Зависимости между сущностями

Некоторые сущности требуют доступа к другим для работы:

```typescript
operations:create → автоматически дает read на:
  - articles
  - accounts
  - counterparties
  - departments
  - deals

budgets:update → автоматически дает read на:
  - articles
  - departments
```

### 3. Централизованная конфигурация

Все сущности определены в **одном месте**: `config/entities.config.ts`

```typescript
export const ENTITIES_CONFIG = {
  operations: {
    name: 'operations',
    displayName: 'Операции',
    category: 'Основные',
    actions: ['create', 'read', 'update', 'delete', 'confirm', 'cancel'],
    requiresReadAccess: [
      'articles',
      'accounts',
      'counterparties',
      'departments',
      'deals',
    ],
  },
  // ... другие сущности
};
```

## Как это работает

### Алгоритм проверки прав

При вызове `permissionsService.checkPermission(userId, companyId, entity, action)`:

1. **Проверка супер-админа** → если да, доступ разрешен
2. **Проверка прямого права** → есть ли `entity:action` в базе данных
3. **Проверка иерархии** → есть ли более высокое право (например, `delete` для `read`)
4. **Проверка зависимостей** → требуется ли `read` для работы с другой сущностью

Результат содержит не только `allowed: boolean`, но и **причину** и **способ получения права**.

### Пример лога

```json
{
  "allowed": true,
  "reason": "Read access granted by dependency: operations requires articles",
  "grantedBy": "dependency"
}
```

## Добавление новой сущности

### Шаг 1: Обновите `entities.config.ts`

```typescript
export const ENTITIES_CONFIG = {
  // ... существующие сущности

  invoices: {
    name: 'invoices',
    displayName: 'Счета-фактуры',
    category: 'Документы',
    actions: ['create', 'read', 'update', 'delete', 'send'],
    description: 'Счета-фактуры для клиентов',
    requiresReadAccess: ['counterparties', 'articles'], // опционально
  },
};
```

### Шаг 2: Добавьте роуты с проверкой прав

```typescript
// invoices.routes.ts
import { requirePermission } from '../../middlewares/permissions';

router.get(
  '/',
  requirePermission('invoices', 'read'),
  invoicesController.getAll
);

router.post(
  '/',
  requirePermission('invoices', 'create'),
  invoicesController.create
);
```

### Шаг 3: Обновите UI (опционально)

```typescript
// Layout.tsx - добавьте в маппинг
const mapping = {
  // ... существующие
  'Счета-фактуры': { entity: 'invoices', action: 'read' },
};
```

### Шаг 4: Готово! 🎉

Система автоматически:

- ✅ Создаст права при инициализации новых компаний
- ✅ Применит иерархию действий
- ✅ Учтет зависимости между сущностями
- ✅ Обеспечит Type Safety (TypeScript подскажет, где нужны изменения)

## Примеры использования

### Backend - Проверка прав в контроллере

```typescript
import permissionsService from '../roles/permissions.service';

// Проверка одного права
const canEdit = await permissionsService.checkPermission(
  userId,
  companyId,
  'operations',
  'update'
);

if (!canEdit) {
  throw new AppError('Access denied', 403);
}

// Получение всех прав пользователя
const permissions = await permissionsService.getUserPermissions(
  userId,
  companyId
);
// { operations: ['create', 'read', 'update'], articles: ['read'], ... }
```

### Backend - Использование middleware

```typescript
import { requirePermission } from '../../middlewares/permissions';

router.post(
  '/operations',
  authenticate,
  extractTenant,
  requirePermission('operations', 'create'), // Автоматически проверяет права
  operationsController.create
);
```

### Frontend - Использование хука

```typescript
import { usePermissions } from '../../shared/hooks/usePermissions';

function OperationsPage() {
  const { canCreate, canUpdate, canDelete } = usePermissions();

  return (
    <>
      {canCreate('operations') && (
        <Button onClick={handleCreate}>Создать операцию</Button>
      )}

      {canUpdate('operations') && (
        <Button onClick={handleEdit}>Редактировать</Button>
      )}

      {canDelete('operations') && (
        <Button onClick={handleDelete}>Удалить</Button>
      )}
    </>
  );
}
```

## Конфигурация иерархии действий

Если нужно изменить иерархию, обновите `config/action-hierarchy.ts`:

```typescript
export const ACTION_HIERARCHY: Record<Action, Action[]> = {
  read: [],
  create: ['read'],
  update: ['read'],
  delete: ['update', 'read'],
  // ... добавьте новые действия
};
```

## FAQ

### Как дать пользователю доступ только на чтение операций?

Создайте роль с правом `operations:read`. Пользователь автоматически получит доступ на чтение всех связанных справочников (articles, accounts, и т.д.).

### Как работает автоматический доступ к справочникам?

Если у пользователя есть `operations:create` или `operations:update`, система автоматически разрешает `read` на все сущности из `requiresReadAccess` (articles, accounts, counterparties, departments, deals).

### Нужно ли обновлять базу данных при добавлении сущности?

Нет! Просто добавьте сущность в `entities.config.ts`. При следующей инициализации новой компании права будут созданы автоматически. Для существующих компаний можно запустить миграцию или обновить права роли вручную через UI.

### Можно ли отключить автоматический резолвинг зависимостей?

Да, просто уберите `requiresReadAccess` из конфигурации сущности. Но это может привести к 403 ошибкам в формах, которым нужны справочники.

## Преимущества этого подхода

✅ **Гранулярность** - каждое право проверяется отдельно  
✅ **Масштабируемость** - добавление сущности = 1 запись в конфиге  
✅ **Поддерживаемость** - вся логика в одном месте  
✅ **Type Safety** - TypeScript контролирует полноту изменений  
✅ **Автоматизация** - иерархия и зависимости работают из коробки  
✅ **Прозрачность** - в логах видно, почему право разрешено/запрещено

## Производительность

Система оптимизирована для production:

- Минимальное количество запросов к базе данных
- Кэширование через глобальный инстанс policy
- Поддержка транзакций для консистентности

---

**Документация обновлена**: 2024
