# Быстрая проверка почтового сервиса

## ✅ 1. Проверка миграции (уже выполнено)

Миграция применена успешно:

```
Database schema is up to date!
```

## 🧪 2. Запуск тестового скрипта

```bash
cd apps/api
pnpm test:email
```

Или с указанием тестового email:

```bash
TEST_EMAIL=your-email@example.com pnpm test:email
```

## 🚀 3. Проверка через API (рекомендуется)

### Запустите API сервер:

```bash
cd apps/api
pnpm dev
```

### Тест регистрации (отправит письмо):

```bash
curl -X POST http://localhost:4000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "test123456",
    "companyName": "Test Company"
  }'
```

**Проверьте почту** - должно прийти письмо с подтверждением!

### Тест восстановления пароля:

```bash
curl -X POST http://localhost:4000/api/auth/forgot-password \
  -H "Content-Type: application/json" \
  -d '{"email": "test@example.com"}'
```

**Проверьте почту** - должно прийти письмо с восстановлением!

## 📋 Чеклист перед деплоем

- [x] Миграция БД применена
- [ ] Переменные окружения настроены (FRONTEND*URL, SMTP*\*)
- [ ] Тестовый скрипт проходит успешно
- [ ] Письма отправляются и приходят на реальный email
- [ ] Ссылки в письмах ведут на правильный домен

## 📖 Подробная инструкция

См. `TESTING_EMAIL_SERVICE.md` для полной инструкции по тестированию.
