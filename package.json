{
  "name": "fin-u-ch",
  "version": "0.1.0",
  "private": true,
  "description": "Financial Management System - Fin-U-CH",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "eslint . --ext .ts,.tsx",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,json,md}\"",
    "type-check": "pnpm -r --parallel type-check",
    "test": "pnpm -r test",
    "test:e2e": "pnpm --filter web test:e2e",
    "prepare": "husky install",
    "env:setup": "node scripts/setup-env.js",
    "env:dev": "node scripts/switch-env.js development",
    "env:prod": "node scripts/switch-env.js production",
    "env:list": "node scripts/switch-env.js --list",
    "env:current": "node scripts/switch-env.js --current",
    "env:check": "node scripts/check-env.js",
    "docker:build": "cd ops/docker && docker compose -f docker-compose.local.yml build",
    "docker:up": "cd ops/docker && docker compose -f docker-compose.local.yml up -d",
    "docker:down": "cd ops/docker && docker compose -f docker-compose.local.yml down",
    "docker:logs": "cd ops/docker && docker compose -f docker-compose.local.yml logs -f",
    "docker:ps": "cd ops/docker && docker compose -f docker-compose.local.yml ps",
    "docker:restart": "cd ops/docker && docker compose -f docker-compose.local.yml restart",
    "docker:clean": "cd ops/docker && docker compose -f docker-compose.local.yml down -v",
    "prisma:generate": "pnpm --filter api prisma:generate && pnpm --filter worker prisma:generate",
    "demo:create": "npx tsx scripts/create-demo-user.ts"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.21.0",
    "@typescript-eslint/parser": "^6.21.0",
    "eslint": "^8.56.0",
    "eslint-config-prettier": "^9.1.0",
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0",
    "prettier": "^3.2.5",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@10.11.0"
}
