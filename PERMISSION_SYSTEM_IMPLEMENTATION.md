# Реализация новой системы прав доступа

## 🎯 Цель

Создать **гранулярную, масштабируемую и легко поддерживаемую** систему прав доступа, которая решает проблемы:

1. ✅ Операции требуют доступа к справочникам (403 ошибки)
2. ✅ Отсутствие иерархии прав (delete не включает read)
3. ✅ Неправильная проверка дашборда
4. ✅ Сложность добавления новых сущностей

## 📦 Что реализовано

### Backend (API)

#### 1. Новая структура модуля прав

```
apps/api/src/modules/roles/
├── types.ts                      # TypeScript типы
├── config/
│   ├── entities.config.ts        # ⭐ ГЛАВНЫЙ ФАЙЛ - конфигурация всех сущностей
│   └── action-hierarchy.ts       # Иерархия действий (delete → update → read)
├── policy/
│   └── permission-policy.ts      # Логика проверки с автоматическим резолвингом
├── permissions.service.ts        # Обновлен для использования policy
├── index.ts                      # Централизованный экспорт
└── README.md                     # Полная документация
```

#### 2. Ключевые возможности

**Иерархия действий**

```typescript
delete → update → read
create → read
archive → read
```

**Зависимости между сущностями**

```typescript
operations:create → автоматически дает read на:
  - articles, accounts, counterparties, departments, deals

budgets:update → автоматически дает read на:
  - articles, departments
```

**Централизованная конфигурация**

- Все сущности в одном файле: `config/entities.config.ts`
- Добавление новой сущности = 5-10 строк кода

#### 3. Обновленные файлы

- ✅ `apps/api/src/modules/roles/types.ts` - **создан**
- ✅ `apps/api/src/modules/roles/config/entities.config.ts` - **создан**
- ✅ `apps/api/src/modules/roles/config/action-hierarchy.ts` - **создан**
- ✅ `apps/api/src/modules/roles/policy/permission-policy.ts` - **создан**
- ✅ `apps/api/src/modules/roles/permissions.service.ts` - **обновлен**
- ✅ `apps/api/src/modules/roles/index.ts` - **создан**
- ✅ `apps/api/src/modules/roles/README.md` - **создан**
- ✅ `apps/api/src/modules/auth/seed-initial-data.ts` - **обновлен**

### Frontend (Web)

#### 1. Исправления

- ✅ `apps/web/src/shared/ui/Layout.tsx` - исправлен маппинг дашборда (`dashboard` вместо `reports`)
- ✅ `apps/web/src/shared/hooks/usePermissions.ts` - добавлена сущность `dashboard`

## 🚀 Как это работает

### Алгоритм проверки прав

```
1. Супер-админ? → ✅ Доступ разрешен
2. Есть прямое право? → ✅ Доступ разрешен
3. Есть более высокое право по иерархии? → ✅ Доступ разрешен
   (например, delete включает read)
4. Запрос read на справочник И есть права на зависимые сущности?
   → ✅ Доступ разрешен
5. Иначе → ❌ Доступ запрещен
```

### Примеры

**Пример 1: Операции**

```
Пользователь имеет: operations:create

Автоматически получает:
✅ operations:read (по иерархии create → read)
✅ articles:read (по зависимости)
✅ accounts:read (по зависимости)
✅ counterparties:read (по зависимости)
✅ departments:read (по зависимости)
✅ deals:read (по зависимости)

Результат: форма операции загружается без 403 ошибок! 🎉
```

**Пример 2: Иерархия**

```
Пользователь имеет: articles:delete

Автоматически получает:
✅ articles:update (по иерархии)
✅ articles:read (по иерархии)

Результат: не нужно давать 3 отдельных права!
```

## 📝 Как добавить новую сущность

### Пример: Добавляем "Счета-фактуры"

**Шаг 1**: Обновите `config/entities.config.ts`

```typescript
export const ENTITIES_CONFIG = {
  // ... существующие

  invoices: {
    name: 'invoices',
    displayName: 'Счета-фактуры',
    category: 'Документы',
    actions: ['create', 'read', 'update', 'delete', 'send'],
    description: 'Счета-фактуры для клиентов',
    requiresReadAccess: ['counterparties', 'articles'], // опционально
  },
};
```

**Шаг 2**: Создайте роуты

```typescript
router.get(
  '/',
  requirePermission('invoices', 'read'),
  invoicesController.getAll
);
```

**Шаг 3**: Готово! ✅

- Права автоматически создадутся при инициализации новых компаний
- Иерархия и зависимости работают автоматически
- TypeScript подскажет, если что-то забыли

## 🔍 Что решено

### Проблема 1: 403 ошибки на справочники

**Было**: При доступе к форме операций прилетают 403 на articles, accounts, counterparties, etc.

**Стало**: Если есть `operations:create` или `operations:update`, автоматически дается `read` на все нужные справочники.

### Проблема 2: Нет иерархии прав

**Было**: Если дать только `update`, то `read` не работает.

**Стало**:

- `delete` автоматически включает `update` и `read`
- `update` автоматически включает `read`
- `create` автоматически включает `read`

### Проблема 3: Дашборд не отображается

**Было**: Использовалась сущность `reports` вместо `dashboard`.

**Стало**: Добавлена сущность `dashboard` с правом `read`, исправлен маппинг в Layout.

### Проблема 4: Сложность добавления сущностей

**Было**: Нужно обновлять множество файлов вручную.

**Стало**: Добавление новой сущности = 1 запись в конфиге (~10 строк).

## 📊 Преимущества

✅ **Гранулярность сохранена** - каждое право проверяется отдельно  
✅ **Масштабируемость** - легко добавлять новые сущности  
✅ **Поддерживаемость** - вся логика в одном месте  
✅ **Type Safety** - TypeScript контролирует полноту  
✅ **Автоматизация** - иерархия и зависимости из коробки  
✅ **Прозрачность** - логи показывают причину разрешения/запрета

## 🧪 Что нужно протестировать

1. **Создание новой компании**
   - Проверить, что роль "Супер-пользователь" имеет все права (включая dashboard)
   - Проверить, что роль "По умолчанию" имеет только read права

2. **Операции**
   - Пользователь с `operations:create` может открыть форму
   - В форме загружаются все выпадающие списки (статьи, счета, контрагенты, подразделения, сделки)
   - Нет 403 ошибок

3. **Иерархия прав**
   - Пользователь с `articles:delete` может читать и редактировать статьи
   - Пользователь с `operations:update` может читать операции

4. **Дашборд**
   - Пользователь с правом `dashboard:read` видит дашборд в меню
   - Дашборд отображается корректно

5. **UI Ролей**
   - При создании/редактировании роли отображаются все сущности по категориям
   - Можно выставить права для каждого действия

## 📚 Документация

Полная документация доступна в:

- `apps/api/src/modules/roles/README.md` - детальное описание системы
- Этот файл - краткая сводка изменений

## 🎉 Результат

Система прав теперь:

- **Гранулярная** - сохранена детальность контроля
- **Гибкая** - автоматически решает зависимости
- **Масштабируемая** - легко добавлять новые сущности
- **Поддерживаемая** - один источник правды

---

**Реализовано**: 21 ноября 2024
