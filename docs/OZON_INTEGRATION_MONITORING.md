# Мониторинг и проверка интеграции Ozon

## 📅 Расписание выполнения

Интеграция Ozon запускается **каждую среду в 00:01 по московскому времени**.

- **Cron расписание**: `1 0 * * *` (каждый день в 00:01)
- **Условие выполнения**: Только если сегодня среда (день недели = 3)
- **Часовой пояс**: `Europe/Moscow`

## 🔍 Способы проверки

### 1. Проверка расписания при запуске Worker

При запуске worker выводится информация о следующем запуске:

```bash
pnpm --filter worker dev
```

В логах вы увидите:

```
⏰ Текущее время: 25.11.2025, 12:00:00
⏰ Дата и время: 26.11.2025, 00:01:00
📊 Дней до запуска: 1
📅 Сегодня вторник (день недели: 2), запускать задачу: НЕТ
```

### 2. Ручной запуск для тестирования

#### Запуск для всех интеграций:

```bash
pnpm --filter worker ozon-now
```

#### Запуск для конкретной интеграции:

```bash
pnpm --filter worker ozon-test <integration-id>
```

### 3. Проверка логов выполнения

#### В режиме разработки:

```bash
pnpm --filter worker dev
```

Ищите в логах:

- `🔄 Запуск генерации операций Ozon...`
- `✅ Успешно создано операций: X`
- `❌ Количество ошибок: X`

#### Пример успешного выполнения:

```
🔄 Запуск периодической задачи генерации операций Ozon
⏰ Время запуска: 26.11.2025, 00:01:00
✅ Сегодня среда - подходящий день для выполнения задачи
🚀 ЗАПУСК ГЕНЕРАЦИИ ОПЕРАЦИЙ OZON
🔍 Начинаем обработку интеграций Ozon...
📊 ИТОГОВЫЕ РЕЗУЛЬТАТЫ ГЕНЕРАЦИИ ОПЕРАЦИЙ OZON
✅ Успешно создано операций: 2
❌ Количество ошибок: 0
🎉 УСПЕХ! Создано 2 операций через интеграцию Ozon!
```

### 4. Проверка через API (если доступен)

```bash
# Проверка статуса интеграции
curl -X GET http://localhost:4000/api/integrations/ozon/status \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### 5. Проверка созданных операций в БД

#### Через Prisma Studio:

```bash
pnpm --filter api prisma:studio
```

Найдите операции с описанием, содержащим `Ozon выплата`.

#### Через SQL запрос:

```sql
SELECT
  id,
  type,
  amount,
  currency,
  "operationDate",
  description,
  "createdAt"
FROM operations
WHERE description LIKE '%Ozon выплата%'
ORDER BY "createdAt" DESC
LIMIT 10;
```

### 6. Проверка через веб-интерфейс

1. Откройте страницу операций
2. Найдите операции с описанием "Ozon выплата"
3. Проверьте дату создания и сумму

## 🧪 Тестирование перед продакшеном

### Шаг 1: Проверка конфигурации

```bash
# Проверьте, что worker запущен
ps aux | grep worker

# Проверьте переменные окружения
cat .env | grep -E "API_URL|WORKER_API_KEY|DATABASE_URL"
```

### Шаг 2: Тестовый запуск

```bash
# Запустите вручную для проверки
pnpm --filter worker ozon-now
```

### Шаг 3: Проверка результатов

```bash
# Проверьте логи
tail -f logs/worker.log | grep -i ozon

# Проверьте созданные операции
pnpm --filter api prisma:studio
```

## 📊 Мониторинг в продакшене

### 1. Настройка алертов

Настройте мониторинг на:

- Отсутствие логов выполнения в среду после 00:05
- Ошибки в логах worker
- Отсутствие созданных операций в среду

### 2. Проверка расписания

Создайте скрипт для проверки следующего запуска:

```typescript
// scripts/check-ozon-schedule.ts
import { getNextRunInfo } from './apps/worker/src/jobs/ozon.generate.operations';

const info = getNextRunInfo();
console.log(`Следующий запуск: ${info.nextRunDate}`);
console.log(`Дней до запуска: ${info.daysUntilNextRun}`);
```

### 3. Логирование в файл

Убедитесь, что worker настроен на логирование в файл:

```typescript
// apps/worker/src/config/logger.ts
// Должен быть настроен file transport
```

## ⚠️ Возможные проблемы

### Проблема: Задача не запускается

**Проверьте:**

1. Worker запущен: `ps aux | grep worker`
2. Сегодня среда: `date +%u` (должно быть 3)
3. Время 00:01 прошло
4. Логи worker на наличие ошибок

### Проблема: Операции не создаются

**Проверьте:**

1. Интеграция активна (`isActive = true`)
2. Есть данные в Ozon API за нужный период
3. `payment < 0` (операции создаются только для отрицательных сумм)
4. Нет дубликатов операций за этот период
5. Указаны `articleId` и `accountId` в интеграции

### Проблема: Ошибки API

**Проверьте:**

1. API доступен: `curl http://localhost:4000/api/health`
2. `WORKER_API_KEY` настроен в `.env`
3. API ключ Ozon валиден
4. Логи API на наличие ошибок

## 🔧 Отладка

### Включение детального логирования

В `apps/worker/src/config/logger.ts` установите уровень `debug`:

```typescript
logger.level = 'debug';
```

### Ручной запуск с отладкой

```bash
# Запуск с выводом всех логов
DEBUG=* pnpm --filter worker ozon-now
```

### Проверка через debug скрипт

```bash
pnpm --filter worker debug-ozon <integration-id>
```

## 📝 Чеклист перед продакшеном

- [ ] Worker запущен и работает
- [ ] Cron расписание настроено правильно
- [ ] `WORKER_API_KEY` настроен
- [ ] API доступен из worker
- [ ] Интеграции Ozon настроены и активны
- [ ] У интеграций указаны `articleId` и `accountId`
- [ ] Логирование настроено
- [ ] Мониторинг настроен
- [ ] Тестовый запуск прошел успешно
- [ ] Операции создаются корректно

## 🎯 Ключевые моменты

1. **Расписание**: Каждая среда в 00:01 МСК
2. **Условие**: Только если `payment < 0`
3. **Период**: Зависит от `paymentSchedule` (next_week или week_after)
4. **Логи**: Все действия логируются в консоль и файл
5. **Fallback**: При недоступности API используется прямой режим работы с БД
