# Как посмотреть полные отчеты AI Code Review

AI Code Review создает подробные отчеты в двух форматах: JSON и Markdown. Вот как их получить:

## 📥 Способ 1: Скачать из GitHub Actions Artifacts

**Для любого PR после внедрения этой фичи:**

1. Откройте PR на GitHub
2. Прокрутите вниз до секции **"Checks"**
3. Найдите workflow **"AI Code Review"** и кликните **"Details"**
4. На странице workflow run прокрутите вниз до секции **"Artifacts"**
5. Скачайте артефакт `ai-review-reports-pr-{NUMBER}`

**Что внутри:**

- `pr-{NUMBER}-{TIMESTAMP}.json` — структурированные данные для парсинга
- `pr-{NUMBER}-{TIMESTAMP}.md` — красиво отформатированный отчет

## 📊 Что содержат полные отчеты

### JSON отчет

```json
{
  "prNumber": 124,
  "timestamp": "2025-11-17T08:53:11.189Z",
  "summary": {
    "total": 58,
    "critical": 4,
    "high": 7,
    "medium": 35,
    "low": 12,
    "withInlinePositions": 25,
    "withoutInlinePositions": 33
  },
  "issues": [
    /* все найденные проблемы */
  ],
  "comments": [
    /* inline комментарии для GitHub */
  ],
  "issuesWithoutInline": [
    /* проблемы вне diff */
  ]
}
```

### Markdown отчет

Включает:

- **Summary** — статистика по severity
- **Critical/High/Medium/Low Issues** — сгруппированные по файлам
- **Issues Without Inline Positions** — проблемы вне измененных строк

## 🎯 Почему есть issues "without inline positions"?

GitHub API позволяет комментировать только измененные строки в PR. Если AI нашел проблему:

- В **неизмененной** строке того же файла
- В файле из **другого батча** (при больших PR)
- На **удаленной** строке

...то эта проблема попадает в раздел "without inline positions" в полном отчете.

## 📝 Способ 2: Inline комментарии в PR

Для быстрого просмотра основных проблем:

1. Откройте PR → вкладка **"Files changed"**
2. Найдите комментарии от **github-actions[bot]**
3. Они содержат категорию, severity и предложение по исправлению

## 💡 Примеры использования

### Найти все критические проблемы:

```bash
# Скачайте JSON, затем:
cat pr-124-*.json | jq '.issues[] | select(.severity == "critical")'
```

### Посмотреть все архитектурные замечания:

```bash
cat pr-124-*.json | jq '.issues[] | select(.category == "architecture")'
```

### Открыть Markdown отчет:

```bash
# Удобно для чтения
cat pr-124-*.md | less
```

## 🔧 Настройки

**Retention period**: 30 дней  
**Формат имени**: `ai-review-reports-pr-{PR_NUMBER}`  
**Расположение в CI**: `.ai-review-reports/` (в gitignore)

## ⚙️ Техническая информация

Артефакты создаются GitHub Actions шагом:

```yaml
- name: Upload AI Review Reports
  if: always() # Даже если review failed
  uses: actions/upload-artifact@v4
  with:
    name: ai-review-reports-pr-${{ github.event.pull_request.number }}
    path: .ai-review-reports/
    retention-days: 30
```

Отчеты сохраняются даже если:

- ❌ Review failed (критические проблемы)
- ⚠️ Workflow был cancelled
- 🔄 Review был повторно запущен

---

**Важно**: Полные отчеты содержат **все** найденные проблемы, включая те, которые не могут быть показаны как inline комментарии в PR!
