# Тестирование функции свернутых окон импорта

## Что было реализовано

Функция позволяет сворачивать окно импорта банковской выписки и видеть свернутое окошко на **всех страницах** авторизованной зоны приложения (Dashboard, Операции, Бюджеты, Отчеты, Справочники и т.д.).

## Как тестировать

### Шаг 1: Открыть окно импорта

1. Перейдите на страницу "Операции" (`/operations`)
2. Нажмите кнопку "Импорт выписки" (иконка FileUp)
3. Окно импорта должно открыться

### Шаг 2: Свернуть окно

1. В открытом окне импорта нажмите кнопку минимизации (горизонтальная линия в правом верхнем углу)
2. Окно должно закрыться
3. В правом нижнем углу экрана должно появиться **свернутое окошко**

### Шаг 3: Проверить видимость на других страницах

1. Перейдите на другие страницы:
   - `/dashboard` (Дашборд)
   - `/budgets` (Бюджеты)
   - `/reports` (Отчеты)
   - `/catalogs/articles` (Статьи)
   - И т.д.
2. На **всех этих страницах** должно быть видно свернутое окошко импорта в правом нижнем углу

### Шаг 4: Развернуть окно

1. На любой странице нажмите кнопку "Развернуть" (иконка Maximize2) на свернутом окошке
2. Вы должны автоматически перенестись на страницу `/operations`
3. Окно импорта должно открыться в том же состоянии, в котором было свернуто

## Типы свернутых секций

### 1. История импортов

- Сворачивается, когда вы находитесь на вкладке "История импортов"
- Показывает текст "История импортов свернута"

### 2. Импортированные операции (таблица маппинга)

- Сворачивается, когда вы загрузили файл и находитесь на экране маппинга операций
- Показывает текст "Таблица маппинга свернута"

### 3. Вкладка загрузки (по умолчанию)

- Если вы сворачиваете окно на вкладке загрузки без активной сессии
- По умолчанию создается свернутая секция "История импортов"

## Отладочные логи

В консоли браузера вы увидите:

- `💾 Сворачиваем окно импорта:` - при сворачивании окна
- `📦 Загружено состояние из localStorage:` - при загрузке состояния
- `✅ CollapsedImportSections: проверка отображения:` - проверка условий отображения
- `🎨 CollapsedImportSections: рендерим свернутые секции!` - когда окошко отображается

## Срок действия

Свернутое состояние сохраняется в localStorage на **24 часа**. После этого оно автоматически удаляется.

## Где НЕ должно быть видно

Свернутые окошки **не должны** отображаться на:

- `/` (Лендинг)
- `/login` (Авторизация)
- `/register` (Регистрация)
- `/forgot-password` и других публичных страницах

Это происходит автоматически, так как `CollapsedImportSections` подключен только в `Layout`, который используется только на авторизованных страницах.
