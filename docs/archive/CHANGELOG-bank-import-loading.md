# Changelog: Индикатор загрузки банковских выписок

## 🎯 Цель изменений

Улучшить пользовательский опыт при загрузке больших банковских выписок, чтобы пользователь:

- Понимал, что идет процесс обработки
- Знал, что это может занять несколько минут
- Мог продолжить работу, свернув окно

## ✨ Что добавлено

### 1. Индикатор загрузки в основном окне

При загрузке файла отображается оверлей с:

- 🔄 Анимированным спиннером загрузки
- 📝 Заголовком "Загрузка выписки..."
- ⏱️ Информацией: "Обработка большой выписки может занять несколько минут"
- 💡 Подсказкой: "Вы можете свернуть это окно, загрузка продолжится в фоновом режиме"

**Визуально:**

```
┌─────────────────────────────────────────────────┐
│                                                 │
│              ⏳ (вращающийся спиннер)           │
│                                                 │
│           Загрузка выписки...                   │
│                                                 │
│   Пожалуйста, подождите. Обработка большой      │
│   выписки может занять несколько минут.         │
│                                                 │
│   Вы можете свернуть это окно, загрузка         │
│   продолжится в фоновом режиме.                 │
│                                                 │
└─────────────────────────────────────────────────┘
```

### 2. Индикатор загрузки в свернутом окне

Если пользователь свернул окно во время загрузки, показывается:

- 🔄 Анимированный спиннер
- 📝 Статус "Загрузка выписки..."
- ⏱️ Подсказка "Это может занять несколько минут"
- 🚫 Кнопка закрытия заблокирована (чтобы не прервать процесс)

### 3. Блокировка drag-and-drop зоны

Во время загрузки:

- Зона загрузки файлов становится неактивной (визуально затемнена)
- Курсор меняется на `cursor-not-allowed`
- Drag-and-drop функции отключены
- Невозможно загрузить второй файл одновременно

## 📊 Технические детали

### Измененные файлы

- `apps/web/src/features/bank-import/BankImportModal.tsx`

### Добавленные зависимости

- `Loader2` иконка из `lucide-react` (уже была в проекте)

### Использованные техники

- RTK Query `isLoading` флаг из мутации
- Conditional rendering в React
- Tailwind CSS для стилей и анимаций
- Backdrop blur эффект для оверлея

### Код изменений

**Добавлен импорт:**

```typescript
import { Loader2 } from 'lucide-react';
```

**Добавлен оверлей загрузки:**

```tsx
{
  isLoading && (
    <div className="absolute inset-0 bg-white/80 dark:bg-gray-900/80 backdrop-blur-sm z-10 ...">
      <Loader2 className="animate-spin ..." size={48} />
      {/* ... сообщения ... */}
    </div>
  );
}
```

**Обновлено минимизированное окно:**

```tsx
{isLoading ? (
  <div className="flex items-center gap-2">
    <Loader2 className="animate-spin ..." />
    <div>
      <p>Загрузка выписки...</p>
      <p>Это может занять несколько минут</p>
    </div>
  </div>
) : /* ... */}
```

## 🎨 Дизайн и UX

### Цветовая схема

- Светлая тема: белый фон с полупрозрачностью, синие акценты
- Темная тема: темно-серый фон с полупрозрачностью, голубые акценты

### Анимации

- Спиннер: плавное вращение (`animate-spin`)
- Backdrop: эффект размытия (`backdrop-blur-sm`)
- Все переходы плавные благодаря Tailwind transitions

### Доступность

- Четкие визуальные индикаторы состояния
- Информативные сообщения
- Поддержка темной темы
- Блокировка нежелательных действий

## 📝 Инструкция для пользователя

### Как работать с большими выписками

1. **Выберите файл выписки**
   - Перетащите файл или кликните для выбора

2. **Дождитесь загрузки**
   - Появится индикатор с информацией о процессе
   - Для больших файлов это может занять 2-3 минуты

3. **Варианты действий:**
   - ✅ **Подождать** - просто оставьте окно открытым
   - ✅ **Свернуть и работать** - нажмите кнопку минимизации (−)
     - Загрузка продолжится в фоне
     - В углу экрана будет индикатор прогресса
     - После завершения окно можно развернуть

4. **После загрузки**
   - Автоматически откроется таблица с импортированными операциями
   - Можно сразу начинать маппинг

### Ограничения

- ⚠️ Максимальный размер файла: **10 MB**
- ⚠️ Максимальное количество операций: **1000**
- ⚠️ Поддерживаемый формат: **только .txt (формат 1С)**

## 🧪 Тестирование

### Тестовые сценарии

#### ✅ Сценарий 1: Небольшой файл

1. Загрузить файл с 10-50 операциями
2. Индикатор должен появиться и быстро исчезнуть
3. Таблица маппинга должна открыться сразу

#### ✅ Сценарий 2: Большой файл

1. Загрузить файл с 500-1000 операциями
2. Индикатор должен показаться с сообщением о времени
3. Загрузка может занять 1-3 минуты
4. После завершения - переход к маппингу

#### ✅ Сценарий 3: Сворачивание во время загрузки

1. Начать загрузку большого файла
2. Нажать кнопку минимизации (−)
3. Окно должно свернуться
4. В углу экрана - индикатор загрузки
5. Можно работать с другими разделами
6. После завершения развернуть окно

#### ✅ Сценарий 4: Попытка загрузить второй файл

1. Начать загрузку файла
2. Попробовать перетащить или выбрать второй файл
3. Зона drag-and-drop должна быть неактивной
4. Второй файл не должен загружаться

#### ✅ Сценарий 5: Темная тема

1. Переключиться на темную тему
2. Повторить сценарии 1-4
3. Все элементы должны быть видны и читаемы

## 🔄 Совместимость

- ✅ Chrome/Edge (последние версии)
- ✅ Firefox (последние версии)
- ✅ Safari (последние версии)
- ✅ Темная и светлая темы
- ✅ Различные разрешения экрана

## 📈 Преимущества нового решения

### До изменений ❌

- Пользователь не знал, что происходит
- UI казался зависшим
- Непонятно, сколько ждать
- Нельзя было продолжить работу

### После изменений ✅

- Понятный индикатор загрузки
- Информация о времени ожидания
- Возможность свернуть и работать дальше
- Защита от случайных действий
- Профессиональный UX

## 🚀 Следующие шаги (опционально)

Возможные улучшения в будущем:

1. **Прогресс-бар** - показывать процент обработки
2. **Уведомления** - push-уведомление при завершении (если окно свернуто)
3. **Статистика в реальном времени** - количество обработанных операций
4. **Отмена загрузки** - кнопка для прерывания процесса
5. **WebSocket** - для действительно больших файлов (>1000 операций)
6. **Фоновая обработка** - через очереди на backend

## 📞 Обратная связь

Если у вас возникли вопросы или предложения по улучшению, пожалуйста, создайте issue в репозитории.

---

**Дата изменений:** 15 ноября 2025  
**Версия:** 1.0.0  
**Автор:** AI Assistant
