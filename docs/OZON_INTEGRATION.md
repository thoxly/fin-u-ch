# Интеграция Ozon - Полное описание работы

## 📋 Обзор

Интеграция Ozon автоматически создает финансовые операции на основе данных о денежных потоках из Ozon Seller API. Интеграция работает только по **средам** и создает операции только при **отрицательном значении payment** (расходы).

## 🏗️ Архитектура

Интеграция состоит из трех основных компонентов:

1. **API сервис** (`apps/api/src/modules/integrations/ozon/ozon-operation.service.ts`)
   - Обрабатывает запросы от worker
   - Выполняет запросы к Ozon API
   - Создает операции в базе данных

2. **Worker сервис** (`apps/worker/src/jobs/ozon.generate.operations.ts`)
   - Планировщик задач (cron)
   - Вызывает API для генерации операций
   - Запускается автоматически по расписанию

3. **Debug скрипт** (`apps/worker/src/scripts/debug-ozon-integration.ts`)
   - Инструмент для отладки и тестирования
   - Может работать в двух режимах: API и Direct

## ⏰ Расписание работы

### Автоматический запуск

- **Частота**: Каждый день в 10:37 (по московскому времени)
- **Условие выполнения**: Только по **средам** (день недели = 3)
- **Функция проверки**: `shouldRunOzonTaskToday()` в `ozon.generate.operations.ts`

```typescript
// Проверка дня недели
export function shouldRunOzonTaskToday(): boolean {
  const today = getCurrentWeekday();
  return today === 3; // Только среда
}
```

### Ручной запуск

```bash
# Через debug скрипт
pnpm run debug-ozon <integration-id>

# Через worker (если настроен)
# Запускается автоматически по cron
```

## 🔄 Процесс работы интеграции

### Шаг 1: Проверка дня недели

- Проверяется, что сегодня **среда**
- Если нет - интеграция не выполняется

### Шаг 2: Определение режима работы (только для debug скрипта)

- **API режим**: Если API сервер доступен (проверка `http://localhost:4000/api/integrations/ozon`)
- **Direct режим**: Прямой доступ к БД и Ozon API (если API недоступен)

### Шаг 3: Получение активных интеграций

- Загружаются все активные интеграции Ozon из БД
- Для каждой интеграции проверяется:
  - `isActive = true`
  - Наличие `clientKey` и `apiKey`

### Шаг 4: Расчет периода запроса данных

Период зависит от поля `paymentSchedule` интеграции:

#### Для `next_week` (выплата на следующей неделе):

- **Период запроса**: Текущая неделя (понедельник - воскресенье текущей недели)
- **Пример для 26.11.2025 (среда)**:
  - Период: **17.11.2025 - 23.11.2025**

#### Для `week_after` (выплата через неделю):

- **Период запроса**: Прошлая неделя (понедельник - воскресенье прошлой недели)
- **Пример для 26.11.2025 (среда)**:
  - Период: **10.11.2025 - 16.11.2025**

### Шаг 5: Проверка дубликатов

- Проверяется, не была ли уже создана операция за этот период
- Поиск по:
  - `companyId`, `articleId`, `accountId`
  - Период `operationDate`
  - Описание содержит "Ozon выплата"

### Шаг 6: Запрос данных из Ozon API

**Endpoint**: `https://api-seller.ozon.ru/v1/finance/cash-flow-statement/list`

**Параметры запроса**:

```json
{
  "date": {
    "from": "2025-11-17T00:00:00.000Z",
    "to": "2025-11-23T23:59:59.999Z"
  },
  "with_details": true,
  "page": 1,
  "page_size": 100
}
```

**Headers**:

- `Client-Id`: API ключ клиента Ozon
- `Api-Key`: API ключ Ozon
- `Content-Type`: application/json

### Шаг 7: Анализ данных и расчет суммы

#### Приоритет 1: Поле `payment` из `details.payments`

```typescript
const paymentAmount = cashFlowData.result.details[0].payments[0].payment;
```

#### Приоритет 2: Fallback расчет (если `payment` отсутствует)

```typescript
const calculatedAmount =
  cashFlow.orders_amount +
  cashFlow.services_amount -
  cashFlow.commission_amount -
  Math.abs(cashFlow.returns_amount);
```

### Шаг 8: Проверка условий создания операции

**Операция создается ТОЛЬКО если**:

1. ✅ `payment < 0` (отрицательное значение)
2. ✅ `payment !== 0`
3. ✅ Нет дубликата операции за этот период
4. ✅ Есть данные в ответе Ozon API

**Операция НЕ создается если**:

- ❌ `payment >= 0` (положительное или ноль)
- ❌ `payment === 0`
- ❌ Уже существует операция за этот период
- ❌ Нет данных в ответе Ozon API

### Шаг 9: Расчет даты операции

Дата операции рассчитывается на основе `paymentSchedule`:

#### Для `next_week`:

- **Дата расчета**: Понедельник после окончания периода
- **Дата выплаты**: Среда следующей недели (дата расчета + 2 дня)

#### Для `week_after`:

- **Дата расчета**: Понедельник через неделю после окончания периода
- **Дата выплаты**: Среда через неделю (дата расчета + 2 дня)

### Шаг 10: Создание операции

**Тип операции**:

- `expense` (расход) - если `payment < 0`
- `income` (доход) - если `payment > 0` (но это не должно происходить по логике)

**Данные операции**:

```typescript
{
  type: 'expense',
  operationDate: paymentDate, // Дата выплаты (среда)
  amount: Math.abs(calculatedAmount), // Абсолютное значение
  currency: 'RUB', // Из ответа Ozon API
  articleId: integration.articleId,
  accountId: integration.accountId,
  companyId: integration.companyId,
  description: `Ozon расход (выплата на следующей неделе) за период 17.11.2025 - 23.11.2025. Сумма: 1 234 RUB`,
  isConfirmed: true
}
```

## 📊 Структура данных

### Модель Integration (Prisma)

```prisma
model Integration {
  id            String   @id
  companyId     String
  type          String   // 'ozon'
  clientKey     String   // Ozon Client-Id
  apiKey        String   // Ozon Api-Key
  paymentSchedule String // 'next_week' | 'week_after'
  articleId     String   // Статья для операций
  accountId     String   // Счет для операций
  isActive      Boolean  @default(true)
  createdAt     DateTime
  updatedAt     DateTime
}
```

### Ответ Ozon API

```typescript
{
  result: {
    cash_flows: [{
      orders_amount: number,
      services_amount: number,
      commission_amount: number,
      returns_amount: number,
      currency_code: string
    }],
    details: [{
      payments: [{
        payment: number,  // ⚠️ Отрицательное значение для расходов
        currency_code: string
      }],
      begin_balance_amount: number,
      end_balance_amount: number
    }]
  }
}
```

## 🔍 Режимы работы (debug скрипт)

### API режим

- Использует API сервер для всех операций
- Требует запущенный API сервер на `http://localhost:4000`
- Преимущества: централизованная логика, единая точка входа

### Direct режим

- Прямой доступ к БД через Prisma
- Прямые запросы к Ozon API
- Преимущества: работает без API сервера, для отладки

## 🛠️ Использование

### Запуск debug скрипта

```bash
cd apps/worker
pnpm run debug-ozon <integration-id>
```

### Пример вывода

```
🔍 Детальная отладка Ozon интеграции: cmi4lbmtg0001s9u7kbnin98p

1. 🔄 Определяем режим работы...
✅ API доступен, используем API режим

2. 🌐 Работаем через API...
   🏥 Проверяем доступность API...
   ✅ API работает корректно
   🔄 Тестируем Ozon API через наш API...
   ✅ Тестовая операция успешно создана через API
   🚀 Создаем операцию через API...
   ✅ Операция успешно создана через API

📊 ИТОГИ ОТЛАДКИ
==================================================
🔧 Режим работы: API
🌐 API доступен: ✅ Да
🎉 ОТЛАДКА ЗАВЕРШЕНА УСПЕШНО!
✅ Операция успешно создана
```

## ⚠️ Важные моменты

1. **Только среда**: Интеграция работает только по средам
2. **Только отрицательные payment**: Операции создаются только при `payment < 0`
3. **Защита от дубликатов**: Проверка существующих операций за период
4. **Таймауты**:
   - API проверка: 3 секунды
   - Ozon API запрос: 30 секунд
   - Worker API запрос: 60 секунд

## 🐛 Отладка

### Проблемы и решения

1. **API недоступен**
   - Проверьте, запущен ли API сервер: `pnpm --filter api dev`
   - Проверьте `API_URL` в `.env` файле worker

2. **Операция не создается**
   - Проверьте, что `payment < 0`
   - Проверьте, нет ли дубликата операции
   - Проверьте логи на наличие ошибок

3. **Неправильный период**
   - Убедитесь, что логика расчета периода соответствует требованиям
   - Проверьте `paymentSchedule` в настройках интеграции

4. **Ошибки Ozon API**
   - Проверьте валидность `clientKey` и `apiKey`
   - Убедитесь, что ключи имеют права на финансовые данные
   - Проверьте лимиты API Ozon

## 📝 Примеры

### Пример 1: next_week, среда 26.11.2025

- **Период запроса**: 17.11.2025 - 23.11.2025 (текущая неделя)
- **Дата операции**: Среда следующей недели (примерно 03.12.2025)
- **Условие**: `payment < 0`

### Пример 2: week_after, среда 26.11.2025

- **Период запроса**: 10.11.2025 - 16.11.2025 (прошлая неделя)
- **Дата операции**: Среда через неделю (примерно 10.12.2025)
- **Условие**: `payment < 0`

## 🔗 Связанные файлы

- `apps/api/src/modules/integrations/ozon/ozon-operation.service.ts` - API сервис
- `apps/worker/src/jobs/ozon.generate.operations.ts` - Worker задачи
- `apps/worker/src/scripts/debug-ozon-integration.ts` - Debug скрипт
- `apps/worker/src/index.ts` - Планировщик cron задач
- `apps/api/src/modules/integrations/ozon/ozon.service.ts` - Базовый Ozon сервис
