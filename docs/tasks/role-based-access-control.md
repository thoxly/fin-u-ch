# Система ролей и прав доступа (RBAC)

## 📋 Тикет для разработчика

**Тип:** Feature

---

## 🎯 Цель

Реализовать гибкую систему ролей и прав доступа для управления пользователями компании с возможностью создания кастомных ролей и назначения прав на уровне сущностей.

---

## 📖 Общий подход к ролевой модели

### 1. Супер-пользователь (Администратор компании)

- **Создание:** Автоматически создаётся при регистрации компании (первый пользователь)
- **Права:** Полный доступ ко всем разделам и функциям системы
- **Возможности:**
  - Изменение настроек компании
  - Управление пользователями и правами
  - Создание и редактирование ролей
  - Доступ ко всем данным компании

### 2. Кастомные роли

- **Гибкость:** Администратор может создать любое количество ролей с произвольными названиями
- **Группировка:** Роли можно сгруппировать по категориям (например, "Бухгалтерия", "Менеджеры отделов", "Наблюдатели")
- **Назначение:** Можно назначать сотрудникам группы ролей (пользователь может иметь несколько ролей)
- **Права:** Определяются на уровне сущностей с детализацией по действиям

### 3. Двухслойная проверка прав

**Принцип:** Права проверяются в двух слоях для надёжности

- **UI-слой (Frontend):**
  - Скрытие/блокировка недоступных элементов интерфейса
  - Визуальная индикация недоступных действий (серые кнопки)
  - Предотвращение отправки недоступных запросов

- **API-слой (Backend):**
  - Обязательная валидация прав на уровне API
  - Отклонение недопустимых действий независимо от UI
  - Логирование попыток неавторизованного доступа

**Важно:** Только UI-проверки легко обойти, поэтому сервер должен отклонять недопустимые действия.

### 4. Миграция существующих пользователей

**Стратегия при первом включении ролей:**

- **Вариант 1:** Назначить всем существующим пользователям роль "Супер-пользователь" или "Администратор"
- **Вариант 2 (рекомендуется):** Автоматически создать роль "По умолчанию" с минимальными правами (только просмотр) и назначить её всем, затем администраторы расширяют права вручную

**Реализация:**

- Миграция БД должна автоматически создавать базовую роль
- Назначение ролей существующим пользователям при применении миграции

### 5. Логирование действий (Audit Log)

**Требования:**

- Хранение журнала действий для аудита
- Информация о каждом действии:
  - Дата и время
  - Пользователь (кто выполнил)
  - Действие (создание/редактирование/удаление)
  - Сущность (операция/бюджет/справочник)
  - Идентификатор изменённой записи
  - Старые и новые значения (для редактирования)

**Применение:**

- Важно для финансового учёта
- Отслеживание изменений операций и бюджетов
- Возможность восстановления данных
- Анализ активности пользователей

### 6. Управление пользователями

**Доступ:** Только администраторы (или пользователи с правом управления пользователями)

**Функционал:**

- Приглашение сотрудников по email
- Редактирование данных пользователей
- Назначение ролей
- Деактивация/активация учётных записей
- Удаление пользователей (с проверкой прав)

**UI-размещение:** Список пользователей в настройках компании (доступен только тем, у кого есть соответствующие права)

### 7. Изоляция данных

**Текущее состояние:** Изоляция по подразделениям, счетам или сделкам не требуется.

**Поведение:** Пользователи с правами "просмотр" видят данные по всей компании.

**Расширение в будущем:** Если появится потребность разделять доступ по подразделениям, можно расширить модель (например, добавить фильтр для ролей "видит только своё подразделение").

---

## 🔐 Права по разделам/сущностям

### Таблица прав доступа

| Сущность / раздел                            | Возможные действия (права)                                                           | Что отображается в интерфейсе                                                                                                                                                                                                                                                                       |
| -------------------------------------------- | ------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Статьи**                                   | Создание, редактирование, архивирование/восстановление, удаление, просмотр           | Список статей с фильтрами по типу (поступления/списания), деятельности и статусу. В каждой строке доступны иконки «изменить», «архивировать» и «удалить».                                                                                                                                           |
| **Счета**                                    | Создание, редактирование, удаление, просмотр                                         | Таблица с наименованием счёта, валютой и признаком активности; кнопки «изменить» и «удалить».                                                                                                                                                                                                       |
| **Подразделения**                            | Создание, редактирование, удаление, просмотр                                         | Таблица с названием и описанием подразделения; действия «изменить» и «удалить».                                                                                                                                                                                                                     |
| **Контрагенты**                              | Создание, редактирование, удаление, просмотр                                         | Таблица с названием, ИНН и категорией; доступно «изменить» и «удалить».                                                                                                                                                                                                                             |
| **Сделки/проекты**                           | Создание, редактирование, удаление, просмотр                                         | Таблица с названием сделки, суммой, контрагентом и подразделением; кнопки «изменить» и «удалить».                                                                                                                                                                                                   |
| **Зарплаты**                                 | Создание записей, редактирование, удаление, просмотр                                 | Раздел для фиксации зарплатных платежей по сотрудникам. Пока в таблице нет данных, но по аналогии будет доступно создание/редактирование/удаление строк.                                                                                                                                            |
| **Операции (финансовые движения)**           | Создание, редактирование, подтверждение/отмена, удаление, просмотр                   | Фильтры по типу операции (поступление/списание/перевод), датам, статье, контрагенту, сделке и подразделению. По нажатию «Создать операцию» открывается форма ввода. После создания строки могут иметь статус черновика/ожидания подтверждения; для них нужны действия «подтвердить» или «отменить». |
| **Бюджеты**                                  | Создание, редактирование, архивирование/восстановление, удаление, просмотр           | Переключатели «Активные / Архивные / Все». Пока бюджетов нет, отображается сообщение «Нет бюджетов» и кнопка «Создать бюджет».                                                                                                                                                                      |
| **Отчёты – ДДС (движение денежных средств)** | Просмотр, экспорт                                                                    | Отчёт формируется по выбранному периоду и режиму (факт/план). Пользователи с правом экспорта смогут выгружать отчёт, например, в Excel или PDF.                                                                                                                                                     |
| **Отчёты – ОПиУ (прибыль и убытки)**         | Просмотр, экспорт                                                                    | Функционал ещё закрыт (иконка «🔒»), но по аналогии с ДДС предполагается просмотр и экспорт.                                                                                                                                                                                                        |
| **Управление пользователями**                | Приглашение/создание, редактирование профиля, назначение ролей, деактивация/удаление | Видно только администраторам. Отдельный экран со списком пользователей и их ролями.                                                                                                                                                                                                                 |

### Детализация действий

**Общие действия:**

- `create` - Создание новой записи
- `read` / `view` - Просмотр записей
- `update` / `edit` - Редактирование записей
- `delete` - Удаление записей

**Специфичные действия:**

- `archive` - Архивирование (для статей и бюджетов)
- `restore` - Восстановление из архива
- `confirm` - Подтверждение операций
- `cancel` - Отмена операций
- `export` - Экспорт данных (для отчётов)

**Справочники:**

- Все справочники имеют стандартный набор: create, read, update, delete
- Статьи дополнительно имеют: archive, restore

**Операции:**

- Стандартные: create, read, update, delete
- Дополнительные: confirm, cancel (для неподтверждённых операций)

**Бюджеты:**

- Стандартные: create, read, update, delete
- Дополнительные: archive, restore

**Отчёты:**

- read - Просмотр отчётов
- export - Экспорт отчётов (Excel, PDF)

**Управление пользователями:**

- `users.create` - Приглашение/создание пользователей
- `users.read` - Просмотр списка пользователей
- `users.update` - Редактирование профилей пользователей
- `users.delete` - Удаление/деактивация пользователей
- `users.manage_roles` - Назначение ролей пользователям

---

## 🗄️ Модель данных

### Новые таблицы в Prisma Schema

```prisma
// Роли
model Role {
  id          String   @id @default(cuid())
  companyId  String
  name        String   // Название роли (например, "Бухгалтер", "Финансовый менеджер")
  description String?  // Описание роли
  category    String?  // Группа ролей (например, "Бухгалтерия", "Менеджеры отделов")
  isSystem    Boolean  @default(false) // Системная роль (нельзя удалить)
  isActive    Boolean  @default(true)

  company     Company  @relation(fields: [companyId], references: [id], onDelete: Cascade)
  permissions RolePermission[]
  userRoles   UserRole[]

  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  deletedAt   DateTime?

  @@index([companyId])
  @@index([companyId, isActive])
}

// Права роли на сущности
model RolePermission {
  id        String   @id @default(cuid())
  roleId    String
  entity    String   // Название сущности (например, "articles", "operations", "budgets")
  action    String   // Действие (например, "create", "read", "update", "delete", "confirm")
  allowed   Boolean  @default(true)

  role      Role     @relation(fields: [roleId], references: [id], onDelete: Cascade)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([roleId, entity, action])
  @@index([roleId])
}

// Связь пользователей и ролей (многие ко многим)
model UserRole {
  id        String   @id @default(cuid())
  userId    String
  roleId    String

  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  role      Role     @relation(fields: [roleId], references: [id], onDelete: Cascade)

  assignedAt DateTime @default(now())
  assignedBy String?  // ID пользователя, который назначил роль

  @@unique([userId, roleId])
  @@index([userId])
  @@index([roleId])
}

// Журнал действий (Audit Log)
model AuditLog {
  id        String   @id @default(cuid())
  companyId String
  userId    String   // Кто выполнил действие
  action    String   // Тип действия (create, update, delete, confirm, etc.)
  entity    String   // Сущность (operation, budget, article, etc.)
  entityId  String   // ID изменённой записи
  changes   Json?    // Изменения (старые и новые значения)
  metadata  Json?    // Дополнительные данные (IP, user agent, etc.)

  company   Company  @relation(fields: [companyId], references: [id], onDelete: Cascade)
  user      User     @relation(fields: [userId], references: [id])

  createdAt DateTime @default(now())

  @@index([companyId])
  @@index([companyId, entity, entityId])
  @@index([companyId, userId])
  @@index([companyId, createdAt])
}

// Расширение модели User
model User {
  // ... существующие поля ...
  userRoles  UserRole[]
  auditLogs  AuditLog[]
  isSuperAdmin Boolean @default(false) // Флаг супер-администратора компании
}
```

### Системные роли (создаются автоматически)

1. **Супер-пользователь** (`super_admin`)
   - Все права на все сущности
   - Управление пользователями
   - Управление ролями
   - Изменение настроек компании

2. **По умолчанию** (`default`)
   - Только просмотр (read) для всех сущностей
   - Без прав на создание/редактирование/удаление

---

## 🛠️ Техническая реализация

### Backend (API)

#### 1. Модуль ролей (`apps/api/src/modules/roles/`)

**Структура:**

```
roles/
├── roles.service.ts      # Бизнес-логика работы с ролями
├── roles.controller.ts   # HTTP endpoints
├── roles.routes.ts       # Маршруты
├── roles.model.ts        # Типы и интерфейсы
└── permissions.service.ts # Логика проверки прав
```

**Endpoints:**

- `GET /api/roles` - Список ролей компании
- `POST /api/roles` - Создание роли
- `GET /api/roles/:id` - Детали роли
- `PUT /api/roles/:id` - Редактирование роли
- `DELETE /api/roles/:id` - Удаление роли
- `GET /api/roles/:id/permissions` - Права роли
- `PUT /api/roles/:id/permissions` - Обновление прав роли
- `POST /api/users/:id/roles` - Назначение ролей пользователю
- `DELETE /api/users/:id/roles/:roleId` - Снятие роли с пользователя

#### 2. Middleware проверки прав (`apps/api/src/middlewares/permissions.ts`)

```typescript
// Пример использования
export const requirePermission = (entity: string, action: string) => {
  return async (req: Request, res: Response, next: NextFunction) => {
    const user = req.user; // Из auth middleware
    const hasPermission = await checkUserPermission(user.id, entity, action);

    if (!hasPermission) {
      return res.status(403).json({
        error: 'Forbidden',
        message: 'У вас нет прав для выполнения этого действия',
      });
    }

    next();
  };
};
```

**Использование в routes:**

```typescript
router.post(
  '/operations',
  auth,
  tenant,
  requirePermission('operations', 'create'),
  operationsController.create
);
```

#### 3. Сервис проверки прав (`apps/api/src/modules/roles/permissions.service.ts`)

```typescript
export class PermissionsService {
  // Проверка прав пользователя на конкретное действие
  async checkPermission(
    userId: string,
    entity: string,
    action: string
  ): Promise<boolean> {
    // 1. Проверка супер-администратора
    // 2. Получение ролей пользователя
    // 3. Проверка прав в ролях
    // 4. Возврат результата
  }

  // Получение всех прав пользователя
  async getUserPermissions(userId: string): Promise<Permission[]> {
    // Возвращает список всех разрешённых действий пользователя
  }
}
```

#### 4. Интеграция с существующими модулями

**Обновление всех контроллеров:**

- Добавить проверку прав перед каждым действием
- Использовать middleware `requirePermission`
- Логировать действия в `AuditLog`

**Пример для операций:**

```typescript
// operations.routes.ts
router.post(
  '/',
  auth,
  tenant,
  requirePermission('operations', 'create'),
  operationsController.create
);

router.patch(
  '/:id',
  auth,
  tenant,
  requirePermission('operations', 'update'),
  operationsController.update
);

router.post(
  '/:id/confirm',
  auth,
  tenant,
  requirePermission('operations', 'confirm'),
  operationsController.confirm
);
```

#### 5. Audit Log сервис

```typescript
export class AuditLogService {
  async logAction(
    userId: string,
    companyId: string,
    action: string,
    entity: string,
    entityId: string,
    changes?: { old?: any; new?: any }
  ): Promise<void> {
    // Сохранение записи в AuditLog
  }
}
```

**Использование:**

```typescript
// В контроллерах после успешного действия
await auditLogService.logAction(
  req.user.id,
  req.companyId,
  'create',
  'operation',
  operation.id,
  { new: operation }
);
```

### Frontend (Web)

#### 1. Хуки для работы с правами (`apps/web/src/shared/hooks/usePermissions.ts`)

```typescript
export const usePermissions = () => {
  const { data: user } = useGetMeQuery();
  const { data: permissions } = useGetUserPermissionsQuery(user?.id);

  const hasPermission = (entity: string, action: string): boolean => {
    // Проверка прав пользователя
  };

  const canCreate = (entity: string) => hasPermission(entity, 'create');
  const canEdit = (entity: string) => hasPermission(entity, 'update');
  const canDelete = (entity: string) => hasPermission(entity, 'delete');
  const canView = (entity: string) => hasPermission(entity, 'read');

  return {
    hasPermission,
    canCreate,
    canEdit,
    canDelete,
    canView,
    // ... другие методы
  };
};
```

#### 2. Компонент защиты от неавторизованного доступа

```typescript
// ProtectedAction.tsx
interface ProtectedActionProps {
  entity: string;
  action: string;
  children: React.ReactNode;
  fallback?: React.ReactNode;
}

export const ProtectedAction: React.FC<ProtectedActionProps> = ({
  entity,
  action,
  children,
  fallback = null,
}) => {
  const { hasPermission } = usePermissions();

  if (!hasPermission(entity, action)) {
    return <>{fallback}</>;
  }

  return <>{children}</>;
};
```

**Использование:**

```typescript
<ProtectedAction entity="operations" action="create">
  <Button onClick={handleCreate}>Создать операцию</Button>
</ProtectedAction>

<ProtectedAction
  entity="operations"
  action="edit"
  fallback={<Button disabled>Редактировать</Button>}
>
  <Button onClick={handleEdit}>Редактировать</Button>
</ProtectedAction>
```

#### 3. Страница управления ролями (`apps/web/src/pages/RolesPage.tsx`)

**Функционал:**

- Список ролей с фильтрацией по категориям
- Создание/редактирование ролей
- Настройка прав в виде таблицы (сущности × действия)
- Управление пользователями и их ролями

**UI:**

- Таблица ролей с названием, категорией, количеством пользователей
- Модальное окно создания/редактирования роли
- Таблица прав (чекбоксы) для каждой роли
- Список пользователей с назначенными ролями

#### 4. Страница управления пользователями (`apps/web/src/pages/UsersPage.tsx`)

**Функционал:**

- Список пользователей компании
- Приглашение пользователей по email
- Редактирование профилей
- Назначение/снятие ролей
- Деактивация/активация пользователей

**UI:**

- Таблица пользователей (email, имя, роли, статус)
- Кнопка "Пригласить пользователя"
- Модальное окно редактирования пользователя
- Выбор ролей для пользователя

#### 5. Интеграция с существующими компонентами

**Обновление всех страниц:**

- Добавить проверку прав перед отображением кнопок
- Использовать `ProtectedAction` для защиты действий
- Скрывать/блокировать недоступные элементы

**Пример для OperationsPage:**

```typescript
const { canCreate, canEdit, canDelete } = usePermissions();

// В рендере
{canCreate('operations') && (
  <Button onClick={handleCreate}>Создать операцию</Button>
)}

{canEdit('operations') && (
  <Button onClick={handleEdit}>Редактировать</Button>
)}

{canDelete('operations') && (
  <Button onClick={handleDelete} variant="danger">Удалить</Button>
)}
```

#### 6. RTK Query endpoints

```typescript
// rolesApi.ts
export const rolesApi = createApi({
  endpoints: (builder) => ({
    getRoles: builder.query<Role[], void>({
      query: () => '/roles',
    }),
    createRole: builder.mutation<Role, CreateRoleDto>({
      query: (data) => ({
        url: '/roles',
        method: 'POST',
        body: data,
      }),
    }),
    updateRole: builder.mutation<Role, { id: string; data: UpdateRoleDto }>({
      query: ({ id, data }) => ({
        url: `/roles/${id}`,
        method: 'PUT',
        body: data,
      }),
    }),
    // ... другие endpoints
  }),
});

// permissionsApi.ts
export const permissionsApi = createApi({
  endpoints: (builder) => ({
    getUserPermissions: builder.query<Permission[], string>({
      query: (userId) => `/users/${userId}/permissions`,
    }),
  }),
});
```

---

## 📝 Миграция БД

### Миграция для добавления ролей

```sql
-- Создание таблиц
CREATE TABLE "Role" (
  id TEXT PRIMARY KEY,
  "companyId" TEXT NOT NULL,
  name TEXT NOT NULL,
  description TEXT,
  category TEXT,
  "isSystem" BOOLEAN DEFAULT false,
  "isActive" BOOLEAN DEFAULT true,
  "createdAt" TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  "updatedAt" TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  "deletedAt" TIMESTAMP
);

CREATE TABLE "RolePermission" (
  id TEXT PRIMARY KEY,
  "roleId" TEXT NOT NULL,
  entity TEXT NOT NULL,
  action TEXT NOT NULL,
  allowed BOOLEAN DEFAULT true,
  "createdAt" TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  "updatedAt" TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  UNIQUE("roleId", entity, action)
);

CREATE TABLE "UserRole" (
  id TEXT PRIMARY KEY,
  "userId" TEXT NOT NULL,
  "roleId" TEXT NOT NULL,
  "assignedAt" TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  "assignedBy" TEXT,
  UNIQUE("userId", "roleId")
);

CREATE TABLE "AuditLog" (
  id TEXT PRIMARY KEY,
  "companyId" TEXT NOT NULL,
  "userId" TEXT NOT NULL,
  action TEXT NOT NULL,
  entity TEXT NOT NULL,
  "entityId" TEXT NOT NULL,
  changes JSONB,
  metadata JSONB,
  "createdAt" TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Индексы
CREATE INDEX "Role_companyId_idx" ON "Role"("companyId");
CREATE INDEX "Role_companyId_isActive_idx" ON "Role"("companyId", "isActive");
CREATE INDEX "RolePermission_roleId_idx" ON "RolePermission"("roleId");
CREATE INDEX "UserRole_userId_idx" ON "UserRole"("userId");
CREATE INDEX "UserRole_roleId_idx" ON "UserRole"("roleId");
CREATE INDEX "AuditLog_companyId_idx" ON "AuditLog"("companyId");
CREATE INDEX "AuditLog_companyId_entity_entityId_idx" ON "AuditLog"("companyId", entity, "entityId");
CREATE INDEX "AuditLog_companyId_userId_idx" ON "AuditLog"("companyId", "userId");
CREATE INDEX "AuditLog_companyId_createdAt_idx" ON "AuditLog"("companyId", "createdAt");

-- Добавление поля isSuperAdmin в User
ALTER TABLE "User" ADD COLUMN "isSuperAdmin" BOOLEAN DEFAULT false;

-- Создание системных ролей для существующих компаний
-- (выполняется в seed-скрипте)
```

### Seed-скрипт для миграции существующих пользователей

```typescript
// scripts/migrate-roles.ts
async function migrateRoles() {
  // 1. Для каждой компании создать роль "Супер-пользователь"
  // 2. Для каждой компании создать роль "По умолчанию" (только просмотр)
  // 3. Первому пользователю компании назначить роль "Супер-пользователь" и установить isSuperAdmin = true
  // 4. Остальным пользователям назначить роль "По умолчанию"
}
```

---

## ✅ Чеклист реализации

### Backend

- [ ] Создать Prisma схему для ролей, прав и аудита
- [ ] Создать миграцию БД
- [ ] Реализовать модуль `roles` (service, controller, routes)
- [ ] Реализовать сервис проверки прав `PermissionsService`
- [ ] Создать middleware `requirePermission`
- [ ] Интегрировать проверку прав во все контроллеры:
  - [ ] Операции
  - [ ] Бюджеты
  - [ ] Справочники (статьи, счета, подразделения, контрагенты, сделки, зарплаты)
  - [ ] Отчёты
  - [ ] Пользователи
- [ ] Реализовать Audit Log сервис
- [ ] Добавить логирование действий во все контроллеры
- [ ] Создать seed-скрипт для миграции существующих пользователей
- [ ] Добавить Swagger документацию для новых endpoints
- [ ] Написать unit-тесты для сервисов
- [ ] Написать integration-тесты для endpoints

### Frontend

- [ ] Создать RTK Query API для ролей и прав
- [ ] Реализовать хук `usePermissions`
- [ ] Создать компонент `ProtectedAction`
- [ ] Создать страницу управления ролями (`RolesPage`)
- [ ] Создать страницу управления пользователями (`UsersPage`)
- [ ] Интегрировать проверку прав во все страницы:
  - [ ] Dashboard
  - [ ] Операции
  - [ ] Бюджеты
  - [ ] Отчёты
  - [ ] Справочники (все)
- [ ] Добавить визуальную индикацию недоступных действий
- [ ] Добавить навигацию к страницам управления ролями/пользователями
- [ ] Реализовать UI для создания/редактирования ролей
- [ ] Реализовать UI для назначения прав (таблица чекбоксов)
- [ ] Реализовать UI для приглашения пользователей
- [ ] Реализовать UI для назначения ролей пользователям

### Тестирование

- [ ] Unit-тесты для сервисов проверки прав
- [ ] Integration-тесты для API endpoints
- [ ] E2E тесты для основных сценариев использования
- [ ] Тестирование миграции существующих пользователей

### Документация

- [ ] Обновить API документацию (Swagger)
- [ ] Обновить документацию проекта
- [ ] Создать руководство пользователя по управлению ролями

---

## 🎨 UI/UX требования

### Интерфейс управления ролями

**Таблица прав:**

- Строки: Сущности (Статьи, Счета, Операции, Бюджеты, Отчёты, Пользователи)
- Столбцы: Действия (Создать, Просмотр, Редактировать, Удалить, Подтвердить, Архивировать, Экспорт)
- Чекбоксы для выбора прав
- Визуальная группировка по категориям действий

**Форма создания/редактирования роли:**

- Поле "Название роли"
- Поле "Описание" (опционально)
- Поле "Категория" (опционально, для группировки)
- Таблица прав с чекбоксами

### Интерфейс управления пользователями

**Список пользователей:**

- Таблица: Email, Имя, Роли, Статус, Действия
- Фильтрация по ролям
- Поиск по email/имени
- Кнопка "Пригласить пользователя"

**Модальное окно приглашения:**

- Поле Email
- Выбор ролей (мультиселект)
- Отправка приглашения

**Модальное окно редактирования пользователя:**

- Редактирование профиля
- Назначение/снятие ролей
- Деактивация/активация

### Поведение UI при отсутствии прав

- **Кнопки создания:** Скрыты или заблокированы (disabled)
- **Кнопки редактирования:** Скрыты или заблокированы
- **Кнопки удаления:** Скрыты или заблокированы
- **Формы:** Недоступные поля заблокированы
- **Визуальная индикация:** Серый цвет для недоступных элементов

---

## 🔄 Дополнительные соображения

### Настройки компании

- Раздел "Настройки компании" доступен только администраторам
- Возможность изменения:
  - Названия организации
  - Базовой валюты
  - Других параметров компании

### Группы и отделы (для будущего расширения)

- Заложить возможность указания в роли списка доступных:
  - Подразделений
  - Сделок
  - Счетов
- Поля в `RolePermission` можно расширить для фильтрации по подразделениям

### Производительность

- Кэширование прав пользователя в Redis
- Инвалидация кэша при изменении ролей
- Оптимизация запросов проверки прав

### Безопасность

- Хеширование чувствительных данных в Audit Log
- Ограничение доступа к Audit Log (только администраторы)
- Защита от SQL injection в проверках прав
- Валидация всех входных данных

---

## 📚 Связанные документы

- [Domain Model](DOMAIN_MODEL.md)
- [Architecture](ARCHITECTURE.md)
- [API Documentation](API.md)

---

## 🚀 План поэтапной реализации

### Этап 1: Базовая инфраструктура (Backend)

1. Создание Prisma схемы
2. Миграция БД
3. Базовые сервисы (Roles, Permissions)
4. Middleware проверки прав

### Этап 2: API и интеграция (Backend)

1. Endpoints для управления ролями
2. Endpoints для управления пользователями
3. Интеграция проверки прав в существующие контроллеры
4. Audit Log сервис

### Этап 3: Frontend - Базовые компоненты

1. RTK Query API
2. Хуки для работы с правами
3. Компонент ProtectedAction
4. Базовая страница управления ролями

### Этап 4: Frontend - UI и интеграция

1. Страница управления пользователями
2. Интеграция проверки прав во все страницы
3. Визуальная индикация недоступных действий
4. UI для назначения прав

### Этап 5: Тестирование и документация

1. Unit и integration тесты
2. E2E тесты
3. Обновление документации
4. Миграция существующих пользователей
